---
import Layout from "../layouts/Layout.astro";
import { Navbar } from "../components/Navbar";
import { DemoForm } from "../components/DemoForm";
import { Footer } from "../components/Footer";
import "../styles/global.css";
---

<Layout title="Solicitar Demo | Fortex">
  <div class="bg-[#0a0a0a] min-h-screen flex flex-col">
    <Navbar client:load />

    <main
      class="flex-grow pt-32 pb-20 relative overflow-hidden flex items-center justify-center"
    >
      {/* Background Effects */}
      <div class="absolute inset-0 z-0 pointer-events-none">
        <div
          class="absolute top-0 left-1/4 w-96 h-96 bg-blue-600/20 rounded-full blur-[128px]"
        >
        </div>
        <div
          class="absolute bottom-0 right-1/4 w-96 h-96 bg-indigo-600/10 rounded-full blur-[128px]"
        >
        </div>
        <div
          class="absolute inset-0 bg-[url('https://grainy-gradients.vercel.app/noise.svg')] opacity-20"
        >
        </div>
      </div>

      <div class="container mx-auto px-4 relative z-10">
        <div class="grid lg:grid-cols-2 gap-12 items-center max-w-6xl mx-auto">
          {/* Left Column: Copy */}
          <div class="space-y-8 text-center lg:text-left">
            <div
              class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs font-bold tracking-wider uppercase"
            >
              <span class="relative flex h-2 w-2">
                <span
                  class="animate-ping absolute inline-flex h-full w-full rounded-full bg-blue-400 opacity-75"
                ></span>
                <span
                  class="relative inline-flex rounded-full h-2 w-2 bg-blue-500"
                ></span>
              </span>
              Agenda tu sesión
            </div>

            <h1
              class="text-4xl md:text-6xl font-bold text-white tracking-tight leading-tight"
            >
              Da el primer paso hacia el <br />
              <span
                class="text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-blue-600"
              >
                orden operativo.
              </span>
            </h1>

            <p class="text-xl text-gray-400 max-w-lg mx-auto lg:mx-0">
              Déjanos tus datos y un experto analizará tu caso para mostrarte
              cómo la Suite OneTex puede resolver tus dolores operativos.
            </p>

            <div class="hidden lg:flex flex-col gap-4 text-gray-500 text-sm">
              <div class="flex items-center gap-3">
                <div
                  class="w-8 h-8 rounded-full bg-white/5 flex items-center justify-center text-blue-500"
                >
                  1
                </div>
                <span>Análisis breve de tu situación actual</span>
              </div>
              <div class="flex items-center gap-3">
                <div
                  class="w-8 h-8 rounded-full bg-white/5 flex items-center justify-center text-blue-500"
                >
                  2
                </div>
                <span>Demo personalizada de la plataforma</span>
              </div>
              <div class="flex items-center gap-3">
                <div
                  class="w-8 h-8 rounded-full bg-white/5 flex items-center justify-center text-blue-500"
                >
                  3
                </div>
                <span>Propuesta de valor clara y transparente</span>
              </div>
            </div>
          </div>

          {/* Right Column: Form */}
          <div>
            <DemoForm client:load />
          </div>
        </div>
      </div>
    </main>

    <Footer client:visible />
  </div>
</Layout>
